<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_shake"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:background="@color/bg"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="company.override.huzykamz.policecopuganda.ShakeActivity">

    <TextView
        android:text="If you need help from your friends or maybe you want to send them an immediate alert
        just turn the switch below on and shake your phone to send them a notification"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="45dp"
        android:textStyle="italic"
        android:textSize="15dp"
        android:gravity="left"
        android:textColor="@color/colorPrimary"
        android:id="@+id/textView2"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Switch
        android:text="@string/switch_text"
        android:textColor="@color/colorPrimary"
        android:textStyle="bold"
        android:layout_width="wrap_content"
        android:textOn="ON"
        android:thumb="@drawable/switch_selector"
        android:track="@drawable/custom_track"
        android:textOff="OFF"
        android:layout_height="wrap_content"
        android:id="@+id/switch1"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
